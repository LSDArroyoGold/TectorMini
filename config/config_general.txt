# ATENCION -- este archivo tiene estado REAL del dispositivo (FIRST_START,
# coordenadas), no es una plantilla. Por eso actualizar_repo.sh lo excluye
# a proposito de la sincronizacion automatica. NUNCA copiar este archivo
# tal cual (scp/cp) de un dispositivo a otro ni del repo a un dispositivo
# ya instalado -- ver la advertencia equivalente en LSD-Tector2.0 (paso
# una vez, 3/9/2026, con FIRST_START).

# Destino: el servidor propio del LSD-Tector por SFTP (ver
# config/rclone.conf.ejemplo). SYNC_REMOTE es el nombre de la seccion de
# rclone.conf; DRIVE_PATH (nombre historico) es la carpeta dentro del
# chroot del usuario SFTP: siempre "data".
SYNC_REMOTE=servidor
DRIVE_PATH=data

# -------------------------

# Retencion de audio LOCAL (ver scripts/limpiar_retencion.sh).
# Cuando el total local supera el limite, se borran carpetas de FECHA ENTERAS empezando por la mas vieja
# -- nunca archivos sueltos de un dia a medias. Corre por cron una vez al
# dia (Tector Mini no tiene cierres de ventana que la disparen).
RETENCION_AUDIO_LOCAL_MB=8192

# -------------------------

# Estado de primer arranque. TRUE activa el hotspot de configuración inicial
FIRST_START=TRUE

# -------------------------

# Nombre y contraseña de la red WiFi de configuración
HOTSPOT_SSID=TectorMini-Setup
HOTSPOT_PASSWORD=lsdtector123

# -------------------------

# Ubicación geográfica de la estación. Coordenadas decimales

## Latitud. Negativo para hemisferio sur
LAT=-34.6131
## Longitud. Negativo para oeste
LON=-58.3772

# -------------------------
